import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const NewlyBorn = props => {
  return (
    <View>
      <Text>Newly Born</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default NewlyBorn;
